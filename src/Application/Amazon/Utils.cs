namespace Application.Amazon;
public static class Utils
{
    // todo: probably move this to a settings file so we can have different bucket keys
    public const string BUCKET_KEY = "aws-psi-bucket";
}

